# gpa_to_percent

## Description

This programs prompts the user for a GPA in decimal format, e.g 4.0, 3.0, etc.
It then outputs the corresponding grade percentage range. this is accomplished by
using a switch and checking each input.

## Installation

- Make sure you have a Java build installed locally
- Clone repo locally and navigate to directory
- Run Main.java file to see program start
